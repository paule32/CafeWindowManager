#include <cafewm.h>

namespace kallup {
CafeWindow::CafeWindow()
{
    // inital values ...
    win_pos.setX(0);
    win_pos.setY(0);
    
    win_pos.setWidth (0);
    win_pos.setHeight(0);
}
}  // namespace: kallup
